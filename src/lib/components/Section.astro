---
import GrainOverlay from "@components/GrainOverlay.astro";

interface Props {
  id?: string;
  grain?: boolean;
}

const { id = "", grain } = Astro.props;
---

<section {id} class="z-1 relative bg-white pt-12">
  <slot />
  {grain && <GrainOverlay />}
</section>
