---
import GrainOverlay from "@components/GrainOverlay.astro";

interface Props {
  id?: string;
  grain?: boolean;
}

const { id = "", grain } = Astro.props;
---

<section {id} class="z-1 relative">
  <slot />
  {grain && <GrainOverlay />}
</section>
